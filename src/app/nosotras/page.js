import { AboutView } from '@/components/home/AboutView';

export const metadata = {
  title: 'Nuestra Historia | Tapetes.pe',
  description: 'Conoce el legado de las artesanas de Contumazá y nuestra misión social.'
};

export default function AboutPage() {
  return <AboutView />;
}
