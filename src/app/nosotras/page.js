import { AboutView } from '@/components/home/AboutView';

export const metadata = {
  title: 'Nuestra Historia | Made In Contumazá',
  description: 'Conoce el legado de los artesanos y productores de Contumazá y nuestra misión social.'
};

export default function AboutPage() {
  return <AboutView />;
}
